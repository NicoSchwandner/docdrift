{
  "name": "docdrift",
  "version": "0.0.2",
  "description": "Link business decisions to source code. Detect when code drifts from documentation.",
  "repository": {
    "type": "git",
    "url": "https://github.com/NicoSchwandner/docdrift.git"
  },
  "type": "module",
  "bin": {
    "docdrift": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "test": "vitest",
    "lint": "eslint src/",
    "prepublishOnly": "npm run build",
    "release": "npm version patch -m 'v%s' && git push && git push --tags && gh release create v$(node -p \"require('./package.json').version\") --generate-notes",
    "release:minor": "npm version minor -m 'v%s' && git push && git push --tags && gh release create v$(node -p \"require('./package.json').version\") --generate-notes",
    "release:major": "npm version major -m 'v%s' && git push && git push --tags && gh release create v$(node -p \"require('./package.json').version\") --generate-notes"
  },
  "keywords": [
    "documentation",
    "context",
    "drift-detection",
    "tree-sitter",
    "code-aware-docs",
    "ai-agent"
  ],
  "author": "Nico Schwandner",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@clack/prompts": "^1.1.0",
    "chalk": "^5.0.0",
    "tree-sitter-wasms": "^0.1.13",
    "web-tree-sitter": "^0.24.0",
    "yaml": "^2.0.0",
    "yargs": "^17.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/yargs": "^17.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
